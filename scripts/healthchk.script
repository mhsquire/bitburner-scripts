
import * as config from "config.script";
import * as lib from "library.script";

while (true) {
	targets = lib.findTargets();

	targets.forEach(function (target) {
		if (!fileExists(config.payload1(), target)){
			scp(config.payload1(), "home", target);
		}
	});
}